import React, { Component } from "react";
import PropTypes from "prop-types";

class Main extends Component {
  render() {
    return <div>123</div>;
  }
}

Main.propTypes = {
  prop: PropTypes.bool
};

export default Main;
